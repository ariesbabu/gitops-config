apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-app
spec:
  rules:
    - host: app-dev.lab.internal
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dev-my-app
                port:
                  number: 80